(function() {
  var config;

  config = (function() {
    function config() {}

    window.config = config;

    config.token = "";

    config.username = "";

    return config;

  })();

}).call(this);
